/*
#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a, b;
	getline(cin,a);
	getline(cin, b);
	decltype(a.size()) i=0, j=0;
	while (i < a.size())
	{
		a[i] = 'X';
		i++;
	}
	cout << a << endl;
	system("pause");
	for (j; j < b.size(); j++)
	{
		b[j] = 'X';
	}
	cout << b << endl;
	system("pause");
	return 0;
}
*/